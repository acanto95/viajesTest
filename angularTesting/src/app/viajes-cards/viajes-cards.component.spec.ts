import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ViajesCardsComponent } from './viajes-cards.component';

describe('ViajesCardsComponent', () => {
  let component: ViajesCardsComponent;
  let fixture: ComponentFixture<ViajesCardsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      
    })
    .compileComponents();
  }));

  beforeEach(() => {
    
  });

  it('should create', () => {
    
  });
});
